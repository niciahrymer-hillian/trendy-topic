# --- Core analysis layer ---
pandas
vaderSentiment
langdetect
python-dotenv
pyarrow

# --- API (React frontend consumes this) ---
fastapi
uvicorn[standard]
httpx          # used by FastAPI's TestClient in tests

# --- Data sourcing (full Hugging Face export, scale path) ---
datasets
huggingface_hub
requests
deep-translator   # default translation provider (free Google endpoint, no key)
argostranslate    # offline fallback when deep-translator is blocked/fails
google-cloud-translate

# --- PostgreSQL scale path (schema in sql/) ---
sqlalchemy
psycopg[binary]

# --- LLM (AI topic extraction) ---
groq           # primary LLM provider (AI bot, topic extraction)
anthropic      # automatic Claude fallback when Groq fails (LLM_PROVIDER to flip primary)
pydantic       # validates the LLM's structured JSON output

# --- Optional / future features ---
elevenlabs     # voice briefings
scikit-learn   # stretch: clustering / similarity

# --- Testing ---
pytest
